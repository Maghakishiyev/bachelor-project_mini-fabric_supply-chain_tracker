# Transaction Generator Configuration
RATE=20              # Transactions per second
SECONDS=300          # Test duration in seconds
CHANNEL=supplychain  # Channel name
CHAINCODE=shipping   # Chaincode name

# Organization Configuration (use existing connection profiles)
ORG_MSP=ManufacturerMSP
PROFILE_PATH=../../network/connection-profiles/connection-manufacturer.yaml

# Gateway Configuration
GATEWAY_PEER=peer0.manufacturer.example.com:7051
CERTIFICATE_PATH=../../network/wallets/manufacturer/admin/signcerts/cert.pem
PRIVATE_KEY_PATH=../../network/wallets/manufacturer/admin/keystore/priv.pem